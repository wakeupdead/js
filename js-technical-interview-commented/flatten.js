/*
 * Функция принимает на вход массив, который может содержать вложенные массивы, которые в свою очередь может содержать
 * встроенные массивы и т.д.
 * Задача: написать функцию, которая будет возвращать плоский массив, созданный из элементов вложенных массивов - т.е.
 * из массива вида [[1, true], { a: 2 }, [[[4], false]]] вернет [1, true, { a: 2 }, 4, false]
 */

function flatten(arr) {
    // code goes here
}
